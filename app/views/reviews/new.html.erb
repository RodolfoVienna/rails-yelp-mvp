<%= simple_form_for [@restaurant, @review] do |f| %>
<!-- The @restaurant must appear before than @review -->
<!-- http://localhost:3000/restaurants/restaurant_id/reviews/new -->
	<%= f.input :content %>
	<%= f.input :rating %>
	<%= f.submit %>
<% end %>
